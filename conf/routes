# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Map static resources from the /public folder to the /assets URL path
GET        /_assets/*file        controllers.Assets.at(path="/public", file)
GET        /_webjars/*file       controllers.WebJarAssets.at(file)

# Default blog
->         /                     controllers.BlogsRouter

